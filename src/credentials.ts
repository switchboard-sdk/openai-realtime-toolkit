/**
 * Switchboard credentials the toolkit falls back to when the app passes none.
 * They're shared defaults — fine to build and test against, but create your own
 * at https://console.switchboard.audio and pass them to
 * `OpenAIRealtimeToolkitProvider` in production.
 */
export const DEFAULT_APP_ID = '6a75f93fc9ccd4a075fa1669'
export const DEFAULT_APP_SECRET = '28b7400732e5dd15e7297764b9cf4d50cc9ec92d'
