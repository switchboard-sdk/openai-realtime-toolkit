# OpenAIRealtimeToolkit example — copy to .env and fill in your key:
#   cp .env.example .env
#
# Your OpenAI API key (the real value lives in .env, which is gitignored).
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
